# Your public domain (must point to this VPS IP)
DOMAIN=bingo.example.com

# Optional: email for Let's Encrypt expiration notifications
# Leave empty to register without email (no notifications)
# EMAIL=admin@example.com
