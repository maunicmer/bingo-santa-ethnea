# SDD Configuration — SantaEthnea
# Auto-generated by sdd-init on 2026-05-20

project: SantaEthnea

context: >
  Vite 8 + React 19 + TypeScript 6 + TailwindCSS 4 SPA.
  State management: Zustand 5. PWA via vite-plugin-pwa.
  Linting: ESLint 10 (flat config) + typescript-eslint + react-hooks + react-refresh.
  Strict TS: noUnusedLocals, noUnusedParameters, erasableSyntaxOnly.
  Architecture: flat components/, single store (stores/gameStore.ts), types/ for domain types.

strict_tdd: false

testing:
  runner: null
  command: null
  coverage_command: null
  layers:
    unit: false
    integration: false
    e2e: false
  recommendation: "Add vitest + @testing-library/react for React component testing"

quality:
  linter:
    available: true
    command: "npm run lint"
  type_checker:
    available: true
    command: "npx tsc -b --noEmit"
  formatter:
    available: false
    command: null

build:
  dev: "npm run dev"
  build: "npm run build"
  preview: "npm run preview"

package_manager: npm
